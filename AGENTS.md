# Kotest-Unit

Refer to `README.md` for this project's details and usage instructions.

## Verification

Always run `./gradlew kotestUnittest` after making changes. A task is considered as done only if it passes the unit
test. This is not equivalent to `./gradlew test`.