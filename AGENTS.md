# Kotest-Unit

Refer to `README.md` for this project's details and usage instructions.

## Verification

Always run tasks listed after making changes:

* `./gradlew kotestUnittest`
    * This is not equivalent to `./gradlew test`.
* `./gradlew ktfmtFormat`

A task is considered as done only if it passes the unit test. 